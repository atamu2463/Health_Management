// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Get a free hosted Postgres database in seconds: `npx create-db`

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
}

// Organizationテーブル
model Organization {
  id        String      @id @default(uuid())
  name      String
}

// Adminsテーブル
model Admins {
  id                String      @id @default(uuid())
  organization_id   Organization      @relation(fields: [id], refarences: [id])
  depertment_id     Depertments       @relation(fields:[id], refarences: [id])
  name              String
  email             String            @unique
  password          String
}

//Depertmentsテーブル
model Depertments {
  id              String        @id @default(uuid())
  organization_id Organization  @relation(fields: [id], refarences: [id])
  name            String
}

// Admin_Employeesテーブル
model AdminEmployees {
  id              String      @id @default(uuid())
  admin_id        Admins  @relation(fields: [id], refarences: [id])
  employees_id    Employees   @relation(fields: [id], refarences: [id])         
}

// Employeesテーブル
model Employees {
  id                String      @id @default(uuid())
  organization_id   Organization      @relation(fields: [id], refarences: [id])
  depertment_id     Depertments       @relation(fields:[id], refarences: [id])
  name              String
  email             String            @unique
  password          String
}

// Healthテーブル
model Health {
  id                String      @id @default(uuid())
  employees_id      Employees   @relation(fields: [id], refarences: [id])
  condition         String
  comment           String
  recoded_date      Datetime
}
